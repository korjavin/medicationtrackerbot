# Placeholder so the jniLibs directory exists in a fresh checkout before any
# build has run. scripts/build-android-binaries.sh populates per-ABI subdirs
# (arm64-v8a/, armeabi-v7a/, x86_64/) with libmedtracker.so.
