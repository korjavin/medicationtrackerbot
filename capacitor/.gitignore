# Generated by `npx cap add ios` / `npx cap add android` — regenerate on a
# fresh checkout. Native project files contain absolute paths and machine-
# specific state, so we don't track them.
ios/
android/

# Node
node_modules/
package-lock.json

# Build artifacts
dist/
www/
