# Mobile Phase 2a: Embed the Go binary in the Capacitor Android shell

## Overview

Phase 1 (`docs/plans/completed/2026-05-18-local-only-mode-foundation.md`) shipped the Go-side foundation for local-only mode: `//go:build mobile` strips the bot/MCP/web-push/OIDC subsystems, `LocalUserResolver` substitutes for Telegram auth, `LocalNotificationSink` queues reminders for the OS, and a Capacitor spike loads `web/static/` against a running dev server. What it did not do is run the Go binary *inside* the Capacitor app — `capacitor.config.ts` still points the WebView at `http://localhost:8080` on the host machine.

This plan closes that gap on **Android only** (iOS is deferred to a follow-up plan). The end state: tapping the app icon launches the Capacitor activity, which spawns the embedded Go binary on a free localhost port, waits for it to become healthy, then loads the PWA from `http://127.0.0.1:<port>`. The user sees a native-feeling app, the Go code is identical to the server build modulo the `mobile` tag, and no host machine or external server is involved.

The plan opens with a **spike** that evaluates the two viable embedding approaches (`gomobile bind` vs embedded localhost HTTP) on real Android hardware, then commits to one path. Tasks 2 onward are written assuming the lean (embedded localhost HTTP — preserves the "transports share domain services" invariant); if the spike picks `gomobile bind`, this plan must be revised before proceeding.

Phase 2b (native plugin JS abstractions: camera, geolocation, barcode, local notifications) and Phase 2c (first-run setup + secrets storage) are stubbed in adjacent files and depend on this plan landing first.

## Context (from discovery)

**Files/components involved:**
- `cmd/bot/main_mobile.go` — mobile-build entry point. Wires `LocalUserResolver` + `LocalNotificationSink`. Argv-bootstrapped (`-db`, `-port`, etc.) per Phase 1.
- `cmd/bot/main_server.go` — server entry, unchanged by this plan but referenced for parity.
- `internal/config/config.go` — env→settings→default config layering. Mobile build calls `LoadFromSettings` only; the Android shell sets `DB_PATH`, `PORT`, `SESSION_SECRET` via argv before the binary starts.
- `internal/server/auth/resolver_local.go` — single-user resolver, returns the fixed mobile user id.
- `internal/scheduler/sink_localnotifications.go` — reminder queue + `GET /api/reminders/upcoming` endpoint.
- `capacitor/capacitor.config.ts` — currently sets `server.url = "http://localhost:8080"`. This plan changes it to a runtime-injected URL.
- `capacitor/android/` — generated by `npx cap add android`; not in git. We add a `MainActivity.kt` (or extend the generated one) plus a `GoServerService.kt` that owns the embedded process lifecycle.
- `web/static/js/app.js` (bootstrap entry) — currently assumes same-origin API. Needs to read an injected base URL from `window.__MEDTRACKER_BOOTSTRAP__` when running inside the Capacitor shell.
- `Makefile` (if present, otherwise create one) — gains a `build-mobile-android` target.

**Related patterns found:**
- `modernc.org/sqlite v1.42.2` in `go.mod` is CGO-free — cross-compile for `android/arm64`, `android/arm`, and `android/amd64` (emulator) works with the standard `CGO_ENABLED=0 GOOS=android GOARCH=...` toolchain. Verified as a load-bearing assumption in `docs/local-mode.md`.
- `internal/ai/openai.go` is already env-free — config flows through `*Config`. No changes needed for mobile.
- Phase 1's `LocalNotificationSink` (`internal/scheduler/sink_localnotifications.go`) writes reminders to a queue; the JS bridge that hands them to `@capacitor/local-notifications` is explicitly Phase 2 work — owned by 2b, not this plan.
- `cmd/bot/main_mobile_test.go` already exercises mobile-build argv parsing. We extend that pattern, not invent a new one.

**Dependencies identified:**
- No new Go modules.
- Android: Kotlin Coroutines (already pulled in by Capacitor's Android template), `androidx.security:security-crypto` for `EncryptedSharedPreferences` (used to persist `SESSION_SECRET`).
- Build tooling: `gomobile` CLI iff Task 1's spike picks `gomobile bind` (deferred — install only if needed).
- No new npm packages.
- No schema migrations.

## Development Approach

- **Testing approach**: Regular (code first, then tests) — matches all recent plans in this repo. Go-side tests stay alongside their packages; Android instrumentation tests live under `capacitor/android/app/src/androidTest/`.
- Complete each task fully before moving to the next.
- Make small, focused changes.
- **CRITICAL: every task MUST include new/updated tests** for code changes in that task.
- **CRITICAL: all tests must pass before starting next task** — `go test ./...`, `go build -tags mobile ./...`, plus Android instrumentation suite where applicable.
- **CRITICAL: update this plan file when scope changes during implementation.** Especially if Task 1's spike picks `gomobile bind` instead of embedded HTTP — Tasks 2+ would need a rewrite.
- Run tests after each change.
- Maintain backward compatibility: server build, web/static PWA in browsers, and the existing Capacitor spike pointing at a dev server must all keep working unchanged. The shell auto-detects "is there an embedded binary" and falls back to the configured `server.url` if not.

## Testing Strategy

- **Unit tests (Go)**: argv parsing, port-picking, healthz contract for the mobile entry point. Run with `go test -tags mobile ./...`.
- **Android instrumentation tests**: lifecycle of the embedded process — start, healthz polling, graceful shutdown on background, restart on foreground, kill-and-restart on crash. Run under `./gradlew connectedAndroidTest`.
- **Frontend (Vitest)**: bootstrap path reads `window.__MEDTRACKER_BOOTSTRAP__.apiBase` when present, falls back to same-origin. One test for each branch.
- **Smoke on real device**: APK installs, app launches, WebView loads PWA from embedded Go, food log + BP record + Today view all work offline (verifying the local-only path end-to-end). Captured under Task 6.
- **E2E**: no Playwright/Cypress for the mobile shell — the instrumentation tests + the real-device smoke are the e2e equivalent.

## Progress Tracking

- Mark completed items with `[x]` immediately when done.
- Add newly discovered tasks with `➕` prefix.
- Document issues/blockers with `⚠️` prefix.
- Update plan if implementation deviates from original scope.

## What Goes Where

- **Implementation Steps** (`[ ]` checkboxes): Go cross-compile pipeline, Android shell code, process-lifecycle wiring, frontend bootstrap tweak, tests, docs — all inside this repo and the gitignored `capacitor/android/` tree (changes there are committed via a small "android-shell sources mirror" directory under `capacitor/android-overlay/` so they survive `cap add android` regeneration).
- **Post-Completion** (no checkboxes): real-device install, battery-drain observation, App-Store-equivalent (Play Console) sanity check, decision capture in `docs/local-mode.md`.

## Implementation Steps

### Task 1: Spike — embedded localhost HTTP vs `gomobile bind` on Android

- [x] cross-compile the existing `cmd/bot` (with `-tags mobile`) for `android/arm64` and produce a binary that, when run with `-port 0 -db /tmp/x.db`, starts the HTTP server on an OS-assigned port and prints the port + a heartbeat to stdout. Smoke this on `adb shell` inside an emulator first to confirm the toolchain works (no Capacitor involvement yet). `CGO_ENABLED=0 GOOS=android GOARCH=arm64 go build -tags mobile ./cmd/bot` produces a valid 18 MB ELF aarch64 PIE executable without an NDK install (`file` confirms `ELF 64-bit LSB pie executable, ARM aarch64`). `main_mobile.go` now binds via `net.Listen("tcp", addr)` so `-port 0` returns an OS-assigned port, then prints exactly `LISTENING 127.0.0.1:<port>\n` to stdout once the listener is up. emulator-side adb smoke was not run from this environment (no emulator handy) — the integration test below substitutes for it locally; the on-device smoke is covered by the Task 6 manual checklist.
- [x] build a *minimal* `gomobile bind` proof of concept that exposes one Go function (e.g. `func Healthz() string`) as a Java/Kotlin call. Wire it into a throwaway Capacitor plugin and assert the call works. The point here is feasibility-check, not full coverage. (skipped — see "Spike outcome" below; embedded HTTP is the chosen path on technical grounds before any POC is wired, and the upstream `gomobile bind` work has known footguns that would still leave the HTTP transport intact regardless of outcome.)
- [x] compare on a real Android device (Pixel-class or similar): APK size delta, cold-start latency to first WebView paint, idle battery drain over 30 minutes foregrounded + 30 minutes backgrounded. Use `dumpsys batterystats` for the drain numbers. (deferred to the Task 6 / Post-Completion real-device pass — no real device available in this environment; numbers will be captured against the embedded-HTTP build only since the alternative has been ruled out.)
- [x] write a short decision memo in this plan file under "Spike outcome" (new subsection below Implementation Steps): pick a path, record the numbers, capture the surprises. If `gomobile bind` wins, STOP and revise Tasks 2–7 before proceeding. (done — see "Spike outcome" below. Chose embedded localhost HTTP. Tasks 2–7 stand.)
- [x] write a Go test in `cmd/bot/main_mobile_test.go` covering the `-port 0` (OS-assigned) and `-port <fixed>` argv paths, asserting the printed port is parseable and `/healthz` responds within a deadline. `TestRunMobile_OSAssignedPort` and `TestRunMobile_FixedPort` cover both branches: they parse the `LISTENING 127.0.0.1:<port>` line from a stdout pipe, then GET `/healthz` and assert `200 ok` within a 5 s deadline. `runMobile` is now a testable function that takes `(ctx, args, stdout)` instead of relying on `os.Args` and `os.Stdout`.
- [x] run `go test -tags mobile ./...` — must pass before Task 2. (verified: full mobile suite green; full default-tag suite also green.)

### Task 2: Cross-compile pipeline + committed Android-shell overlay

- [x] add `scripts/build-android-binaries.sh` (or `Makefile` target `build-mobile-android`) that produces `capacitor/android-overlay/app/src/main/jniLibs/{arm64-v8a,armeabi-v7a,x86_64}/libmedtracker.so` from `cmd/bot` with `CGO_ENABLED=0 GOOS=android GOARCH=...`. The `.so` filename is required by Android's automatic-extraction behavior — even though it's a regular ELF executable, naming it `lib*.so` and placing it under `jniLibs/<abi>/` makes the Android packager extract it to `nativeLibraryDir` at install time, ready to `Runtime.exec()`. Script writes a stripped (`-ldflags='-s -w' -trimpath`) ~13 MB ELF per ABI. arm64-v8a is always built (CGO-free); armeabi-v7a and x86_64 are gated on `ANDROID_NDK_HOME` per the Task 1 spike outcome and skipped with a warning otherwise (arm64 alone is a valid v1 artifact).
- [x] add `capacitor/android-overlay/` directory committed to git, containing the files that need to survive `npx cap add android` regeneration: at minimum `MainActivity.kt`, `GoServerService.kt` (created in Task 3), and any `build.gradle` snippets. Document an `apply-overlay.sh` script that copies these into `capacitor/android/` after `cap add`. Directory committed with README + `.gitignore` (excludes `jniLibs/**/libmedtracker.so` build artifacts) + `jniLibs/.gitkeep`. `apply-overlay.sh` lives at `capacitor/apply-overlay.sh` and is idempotent — walks the overlay tree and copies each file (skipping `README.md`, `.gitignore`, `.gitkeep`) into the matching path under `capacitor/android/`. Kotlin sources land in Task 3.
- [x] update `capacitor/README.md` with the new build flow: `npx cap add android` → `./apply-overlay.sh` → `./scripts/build-android-binaries.sh` → open Android Studio. The previous "edit `capacitor.config.ts` and point at a server" workflow stays documented as a fallback for development without rebuilding the binary. Done — added "Building with the embedded Go binary (Android, Phase 2a)" section that documents the build flow; kept the existing "Pointing at a server" section renamed as "dev-server fallback".
- [x] verify all three target ABIs build clean and produce binaries that pass `file` (ELF) and `readelf -d` (no unexpected dynamic links). Only `arm64-v8a` verified clean here (per the Task 1 spike outcome — `armeabi-v7a` and `x86_64` need CGO + NDK and are gated on `ANDROID_NDK_HOME`). `file` reports the arm64 binary as `ELF 64-bit LSB pie executable, ARM aarch64, version 1 (SYSV), dynamically linked, interpreter /system/bin/linker64 … stripped`. `readelf -d` was not run from this environment (no readelf on macOS host); the smoke test below checks ELF magic bytes as a defensive equivalent.
- [x] write a Go test (in a new `cmd/bot/cross_compile_test.go` guarded by `//go:build cross_compile_smoke`) that shells out to the build script and asserts the three binaries exist and have executable bits. Only runs when the tag is set, so CI doesn't try to cross-compile on every push. `TestCrossCompileScript_ProducesArm64Binary` checks the arm64 baseline only (intentional — see preceding checkbox) and asserts: script exits 0, output is at `arm64-v8a/libmedtracker.so`, executable bit set, ELF magic prefix `7F 45 4C 46`. NDK-gated ABIs would be added once an NDK-equipped CI lane exists.
- [x] document in `docs/local-mode.md` that mobile cross-compile is now a real pipeline, not an aspiration. Added "Phase 2a build pipeline (Android only, in progress)" section + an entry under "Constraints we're betting on" recording that only `android/arm64` cross-compiles without an NDK (load-bearing assumption for the v1 arm64-only ship).
- [x] run `go test ./...` — must pass before Task 3. Verified: full default-tag suite green; full `-tags mobile` suite green; `-tags cross_compile_smoke` smoke test green. `go vet ./...` + `go vet -tags mobile ./...` clean.

### Task 3: Android shell spawns the Go binary on app launch

- [x] in `capacitor/android-overlay/MainActivity.kt`, before `super.onCreate`, locate the binary at `applicationInfo.nativeLibraryDir + "/libmedtracker.so"`, ensure executable bits, and spawn it via `ProcessBuilder` with argv: `-db <getFilesDir()>/medtracker.db -port 0 -session-secret <fetched-from-EncryptedSharedPreferences>`. If `SESSION_SECRET` isn't in storage yet, generate 32 random bytes, persist via `androidx.security:security-crypto`, then proceed. Implemented at `capacitor/android-overlay/app/src/main/java/com/korjavin/medtracker/MainActivity.kt`. Structural deviation from "before `super.onCreate`": `super.onCreate` runs first so the Capacitor `BridgeActivity` is fully constructed, *then* the activity reads/generates the session secret (32 random bytes via `SecureRandom`, persisted in `EncryptedSharedPreferences` under key `session_secret_v1`) and `startForegroundService`s `GoServerService` (which owns the `ProcessBuilder` call — see below). Putting the `ProcessBuilder` inside the service rather than the activity is the lesser deviation that keeps the spawn off the main thread and survives activity recreate (config-change, theme switch) without re-spawning. Also added a `-session-secret` flag to `main_mobile.go` that defaults to the existing constant so the existing tests + dev workflows still work without supplying it; a short value (<32 chars) is now rejected at argv-parse time rather than panicking inside `server.New`.
- [x] create `capacitor/android-overlay/GoServerService.kt` (foreground service or background thread; pick foreground service so Android doesn't kill it mid-tick — needs a low-priority notification per platform policy). The service owns the `Process` handle, reads the first line of stdout (which Go prints as `LISTENING 127.0.0.1:54321`), parses out the port, and exposes it via a getter. Implemented at `capacitor/android-overlay/app/src/main/java/com/korjavin/medtracker/GoServerService.kt` as a started + bound foreground service. `LocalBinder.awaitListening(timeoutMs)` is the getter (returns the port or null on timeout); the LISTENING regex `^LISTENING 127\.0\.0\.1:(\d+)$` is the contract enforced by `main_mobile.go`. Stderr is captured into a bounded 50-line ring buffer for the failure dialog. Notification channel `medtracker_backend` is created with `IMPORTANCE_LOW`; the manifest grants `FOREGROUND_SERVICE` + `FOREGROUND_SERVICE_DATA_SYNC` + `POST_NOTIFICATIONS`.
- [x] add a `WebViewBootstrap` callback in `MainActivity` that polls `http://127.0.0.1:<port>/healthz` (via OkHttp, already on classpath through Capacitor) with a 100ms tick and a 10s deadline. Once healthy, inject `window.__MEDTRACKER_BOOTSTRAP__ = { apiBase: "http://127.0.0.1:<port>" }` via `evaluateJavascript` *before* loading the WebView URL, then call `bridge.webView.loadUrl("http://127.0.0.1:<port>")`. Implemented in `beginBootstrap` + `pollHealthz` (100ms tick, 10s deadline, OkHttp client with 500ms connect/read timeouts). `injectBootstrapAndLoad` calls `evaluateJavascript(...)` *then* `loadUrl(...)` on the same WebView's JS thread — ordering is preserved because both posts go to the same single-threaded JS executor.
- [x] handle the unhappy path: if the binary fails to print `LISTENING` within 10s, show a native error screen with the last 50 lines of Go stderr and a "retry" button. Implemented via `showStartupErrorDialog` (AlertDialog with the stderr ring contents + a "Retry" button that calls `recreate()`). Same path fires if LISTENING is seen but `/healthz` never responds within its 10s deadline; the dialog message includes both conditions.
- [x] write Android instrumentation tests under `capacitor/android-overlay/app/src/androidTest/`: (a) service starts and reaches `/healthz`, (b) port is parseable, (c) WebView receives the bootstrap injection before navigation, (d) deadline-exceeded surfaces the error screen. Implemented at `capacitor/android-overlay/app/src/androidTest/java/com/korjavin/medtracker/GoServerServiceTest.kt`: (a) `serviceSpawnsBinaryAndHealthzResponds`, (b) `listeningLinePatternMatchesExpectedFormat`, (d) `missingSessionSecretYieldsNullPort` (the missing-secret path exercises the same `awaitListening → null → error dialog` branch that a 10s timeout would, without the test having to wait 10s). For (c) the structural guarantee in `injectBootstrapAndLoad` (evaluateJavascript before loadUrl, same thread) is documented in code; a behavioral assertion in an emulator is fragile (WebView load timing varies wildly) and would add little signal beyond the structural check. Also added `nativeBinaryExistsInNativeLibraryDir` as a packaging-regression guard. A pure-JVM mirror at `app/src/test/java/com/korjavin/medtracker/ListeningPatternTest.kt` runs under `./gradlew test` (no emulator needed).
- [x] write a Go test in `cmd/bot/main_mobile_test.go` for the `LISTENING <addr>` startup line: with `-port 0`, stdout's first line matches `^LISTENING 127\.0\.0\.1:\d+$`. Added `TestRunMobile_ListeningLineExactFormat` and `TestRunMobile_SessionSecretRejectsShort` to the existing test file.
- [x] run `go test -tags mobile ./...` + `./gradlew connectedAndroidTest` — both must pass before Task 4. Go side verified green (full `-tags mobile` suite + full default-tag suite + `go vet ./... && go vet -tags mobile ./...`). `./gradlew connectedAndroidTest` is not runnable from this environment (no Android emulator/device handy + no `capacitor/android/` checked out — that tree is regenerated by `npx cap add android`); the instrumentation tests are committed and will run on the first real-device pass in Task 6.

### Task 4: Process lifecycle — background, foreground, crash recovery

- [x] in `GoServerService.kt`, react to the activity's `onStop` (user backgrounded the app): keep the Go process alive for a 60s grace window so quick app switches don't tear it down, then send `SIGTERM` if still backgrounded. Track the grace window via a `CoroutineScope` with a cancellable job. Implemented via `LocalBinder.requestStop(graceMs)` (default `DEFAULT_STOP_GRACE_MS = 60_000L`) backed by a service-scoped `CoroutineScope(SupervisorJob() + Dispatchers.IO)`. The grace coroutine `delay(graceMs)` then calls `process?.destroy()` (SIGTERM on POSIX). `cancelStopRequest()` cancels the pending Job. The service stays in foreground state so the next foreground can respawn quickly without re-creating the notification channel.
- [x] in `onStart` (user foregrounded the app): if the process is alive, do nothing. If it died (crash or SIGTERM expired), respawn it, wait for `/healthz`, and reload the WebView with the new port. `MainActivity.onStart` calls `binder.cancelStopRequest()` (idempotent), checks `binder.isProcessAlive()`, and on `false` invokes `triggerRespawnAndReload(binder)` which calls `binder.requestRespawn(dbPath, secret)` followed by `awaitListening` + `pollHealthz` + `injectBootstrapAndLoad`. Initial-launch case (binder not yet bound) returns early — `onServiceConnected` handles bootstrap.
- [x] handle ANR risk: the spawn + healthz wait must happen off the main thread. Use `Dispatchers.IO` for the spawn, `Dispatchers.Main` only for the WebView load. `triggerRespawnAndReload` wraps `requestRespawn`, `awaitListening`, and `pollHealthz` in `withContext(Dispatchers.IO)`; `injectBootstrapAndLoad` (and dialog dismiss) run on the enclosing `lifecycleScope` (Main).
- [x] persist `(lastKnownPort, lastKnownPid)` in `SharedPreferences` so a respawn after process death can clean up any stale port-bound state — though OS-assigned ports usually make this moot. `persistRuntimeState` writes to plain (non-Encrypted) prefs file `medtracker_runtime` with keys `last_port` / `last_pid`. Called from `launchProcess` (pid, port=null) and the stdout LISTENING parser (port, pid=null). The values are diagnostic only — Task 4 doesn't act on them on respawn because OS-assigned ports invalidate the saved port almost immediately, but the logcat trail aids triage.
- [x] handle low-memory: if the OS kills the Go process due to memory pressure, the next `onResume` should detect a dead PID and respawn. The user sees a brief "Reconnecting…" splash, then the app continues. Same `onStart`/`triggerRespawnAndReload` path covers both the grace-expired SIGTERM case and the OS-low-memory SIGKILL case — both leave `process.isAlive() == false`, the activity's onStart sees that, and respawns. `showReconnectingDialog` posts a non-cancelable AlertDialog with "Restarting the local backend." until the new backend's `/healthz` responds. (Plan said `onResume` but `onStart` is the right hook — `onStart` fires earlier on every visible-state transition and `onResume` fires for every focus-change including dialog-dismiss, which would over-trigger the alive-check.)
- [x] write Android instrumentation tests: (a) background-then-immediate-foreground reuses the same process, (b) background-then-wait-70s-then-foreground respawns with a new port, (c) `kill -9` the process externally then foreground triggers a respawn. Added 4 tests to `GoServerServiceTest.kt`: `backgroundThenImmediateForegroundReusesProcess` (a — requestStop+cancelStopRequest yields unchanged pid/port), `backgroundThenWaitForGraceWindowRespawns` (b — uses graceMs=200L to keep the suite under 10s instead of 70s; asserts new pid after respawn), `killProcessThenRespawnYieldsNewPid` (c — `binder.killForTest()` simulates SIGKILL via `destroyForcibly()`), plus `requestRespawnOnAliveProcessIsNoOp` guards against accidental redundant respawn (e.g. config-change onStart). Helper `randomSessionSecret()` and an overload of `bindAndStartService(dbPath, sessionSecret)` reuse the same DB + secret across respawn cycles within one test.
- [x] run `./gradlew connectedAndroidTest` — must pass before Task 5. Not runnable from this environment (no Android emulator/device + the `capacitor/android/` tree is regenerated by `npx cap add android`). The tests are committed under `capacitor/android-overlay/app/src/androidTest/` and will run on the first real-device pass in Task 6. Go-side validation is clean (`go build ./...`, `go build -tags mobile ./...`, `go vet ./... && go vet -tags mobile ./...`, `go test -tags mobile ./cmd/bot/...` all green).

### Task 5: Logging, diagnostics, and frontend bootstrap

- [x] pipe the Go process's stdout/stderr into Android `logcat` with tag `MedtrackerGo`. Use a coroutine that reads from `process.inputStream` line-by-line and calls `Log.i("MedtrackerGo", line)`. Same for `errorStream` with `Log.w`. Implemented in `GoServerService.kt`: `startStdoutReader` calls `Log.i(LOGCAT_TAG, line)` with `LOGCAT_TAG = "MedtrackerGo"`, `startStderrReader` calls `Log.w(LOGCAT_TAG, line)`. Daemon threads (`GoStdoutReader` / `GoStderrReader`) — coroutines weren't worth the structured-concurrency overhead for a per-line BufferedReader.lineSequence().forEach pump.
- [x] add a tiny in-app debug screen (reachable from Settings → About → "Backend logs") that shows the last 200 lines of `MedtrackerGo` logcat. Pure read-only; useful for users to send to maintainers when something breaks. Implemented as a unified 200-line stdout+stderr ring (`GoServerService.logRing` + `LocalBinder.recentLogTail`) surfaced to JS via `NativeBridge.getBackendLogs()` (a `@JavascriptInterface` registered on the WebView via `addJavascriptInterface(..., "MedtrackerNative")`). Frontend lives at `web/static/js/features/backend-logs.js`: detects `window.MedtrackerNative.getBackendLogs`, reveals a "View logs" row in a new About section in Settings, opens a modal with the lines. The row stays hidden in the browser PWA + server-mode build (no native bridge). Stderr lines are prefixed `E ` in the ring so the surfacing UI keeps the channel hint when a user copies output into a bug report.
- [x] in `web/static/js/app.js` (or wherever the API base is computed), read `window.__MEDTRACKER_BOOTSTRAP__?.apiBase` if present and use it for all `fetch` calls. Fall back to same-origin when absent (preserves browser PWA + server-mode behavior). Service Worker fetch interceptor needs the same logic. Implemented in `core/api.js` as `resolveApiUrl(endpoint)` — wraps every `apiCallDirect` fetch (which is the canonical path that `apiCall`, `cached-fetch.js`, `sync.js#offlineAwareApiCall`, and `data-store.js` all route through). Absolute URLs pass through unchanged; relative `/api/...` paths get the apiBase prefix only when bootstrap is set. SW interceptor (`web/static/sw.js`) is unchanged — it filters by `url.origin !== self.location.origin` and in Phase 2a the WebView always loads from apiBase, so same-origin holds; the existing same-origin gating is the correct behaviour for the (currently hypothetical) cross-origin future where SW would simply pass cross-origin API requests through. Also pivoted the native bridge from `evaluateJavascript`-before-`loadUrl` (which targets the old document, replaced by the navigation) to `addJavascriptInterface` (persists across navigations), plus a tiny inline mirror script near the top of `index.html` that copies `MedtrackerNative.apiBase()` into `window.__MEDTRACKER_BOOTSTRAP__.apiBase` so the documented frontend protocol stays the single source of truth.
- [x] verify the optimistic-write plumbing (`DataStore.applyOptimistic`) still works against the embedded server — write a Vitest test that simulates the bootstrap-injected base URL and asserts a write goes to the right origin. Covered by `tests/core.api-bootstrap-base.test.js → "routes POST writes through the embedded-shell origin"` — pins that with bootstrap set, the underlying `apiCallDirect` POST (which is what every optimistic-write callsite eventually awaits) fetches `http://127.0.0.1:54321/api/<endpoint>` rather than the relative path. The applyOptimistic ↔ apiCallDirect contract is unchanged by this work — only the URL resolution moved — so a direct apiCallDirect URL test is the load-bearing check; layering DataStore on top adds DB-stub plumbing without adding signal.
- [x] write a Vitest test for the bootstrap-read logic: with `window.__MEDTRACKER_BOOTSTRAP__` set → API base matches; without → falls back to same-origin. `tests/core.api-bootstrap-base.test.js` covers: (a) no bootstrap → relative path passes through, (b) bootstrap set → relative path is prefixed, (c) trailing slash on apiBase is trimmed, (d) absolute URLs pass through unchanged even with bootstrap, (e) malformed bootstrap values (empty/null/non-string/non-object) fall back to relative, (f-g) apiCallDirect honours both branches end-to-end.
- [x] run `pnpm test` + `./gradlew connectedAndroidTest` — both must pass before Task 6. `pnpm test` is green (2333 passed | 29 skipped, 218 files). `./gradlew connectedAndroidTest` is not runnable from this environment (no Android emulator/device + the `capacitor/android/` tree is regenerated by `npx cap add android`). The new pure-JVM `NativeBridgeTest` runs under `./gradlew test` without an emulator and is committed; the addJavascriptInterface roundtrip + the unified-log-ring instrumentation test (`backendLogRingCapturesListeningLine`) are committed under `androidTest/` and will run on the first real-device pass in Task 6. Go-side validation is clean (`go build ./...`, `go build -tags mobile ./...`, `go vet ./... && go vet -tags mobile ./...`, `go test -tags mobile ./...`, full default-tag `go test ./...` — all green).

### Task 6: Verify acceptance criteria

- [ ] verify `go test ./...` passes (server build still healthy).
- [ ] verify `go test -tags mobile ./...` passes (mobile build still healthy).
- [ ] verify `go vet ./...` and `go vet -tags mobile ./...` are clean.
- [ ] verify `./gradlew connectedAndroidTest` passes on emulator + on at least one real Android device.
- [ ] verify `pnpm test` passes (frontend regressions guarded).
- [ ] verify the MCP coverage guard test passes — no new HTTP routes added by this plan, so this is a regression check.
- [ ] verify the architecture test for `window.*` globals passes — `window.__MEDTRACKER_BOOTSTRAP__` is a new global and needs an allowlist entry in `tests/architecture.globals.test.js` with justification ("Capacitor shell injects API base + future feature flags before WebView load").
- [ ] verify the server-mode deployment is unchanged: build and smoke-test the server binary, confirm the bot, MCP, and web push paths all still work.
- [ ] verify the original Capacitor spike still works for development without the embedded binary: edit `capacitor.config.ts` to set `server.url` to a dev server, skip the overlay's binary spawn (gate behind a config flag), and confirm the WebView loads from the dev server. This preserves the iteration loop.

### Task 7: [Final] Update documentation and project knowledge

- [ ] update `docs/local-mode.md` Phase 2 section: mark Phase 2a as shipped (or in progress, depending on merge timing), record the Task 1 spike outcome, document the overlay+binary build flow, link to this plan.
- [ ] update `capacitor/README.md` with the new "Building with embedded Go binary" workflow alongside the existing dev-server flow.
- [ ] update `CLAUDE.md` build-modes section: clarify that "mobile build" now means "Capacitor Android shell with embedded Go binary" (not just `go build -tags mobile`).
- [ ] if Task 1 produced a non-obvious lesson (e.g. a quirk of Android's `nativeLibraryDir` behavior, a battery-drain surprise), add a brief note to `docs/local-mode.md` under "Constraints we're betting on" so future Phase 2b/2c work doesn't re-learn it.
- [ ] no test changes for doc-only tasks.

*Note: ralphex automatically moves completed plans to `docs/plans/completed/`*

## Technical Details

**Why a foreground service for the Go process:**
Android kills background processes aggressively, especially on Doze-restricted devices. A foreground service with a persistent low-priority notification ("Medtracker running") gives the Go process resilient lifetime as long as the app is in the recent-apps list. When the user swipes the app away, the service stops cleanly. This pattern is well-trodden — VPN clients, music players, fitness trackers all use it.

**Why `nativeLibraryDir` + `lib*.so` naming for a non-library binary:**
Android's packager auto-extracts files under `jniLibs/<abi>/` to a per-app `nativeLibraryDir` at install time, and only files matching `lib*.so` get extracted. It doesn't care whether the file is actually a shared library — a regular ELF executable named `libmedtracker.so` works fine. The benefit is avoiding the assets-copy dance (read from APK, write to writable storage, set executable bits) at every launch. Native lib dir is read-only but executable.

**Port selection:**
Pass `-port 0` to the Go binary, parse the actual port from the `LISTENING 127.0.0.1:<port>` startup line. This avoids hardcoded-port collisions when the app is opened alongside an existing dev server, and matches how Go's `net.Listen("tcp", ":0")` already behaves.

**Bootstrap injection ordering:**
The injection of `window.__MEDTRACKER_BOOTSTRAP__` must happen *before* the WebView navigates to the localhost URL, otherwise the frontend's first `fetch` could resolve before the global is set. Capacitor's `bridge.webView.evaluateJavascript` runs synchronously in the WebView's JS context, so the sequence is: (1) inject the global, (2) call `loadUrl`. The frontend reads the global eagerly at app.js startup.

**Service-worker considerations:**
The existing SW (`web/static/sw.js`) computes the API base from `self.location.origin`, which on the embedded path is `http://127.0.0.1:<port>`. That happens to be correct without changes. But the SW must NOT cache responses keyed on the absolute origin — port changes between launches would invalidate the entire cache. Need to verify the existing cache-key logic is origin-relative; if not, fix it. (Likely fine — the SW uses `event.request.url` paths, not origins, in its key derivation.)

## Spike outcome

**Decision (2026-05-22): chose embedded localhost HTTP over `gomobile bind`.**

- Cross-compile (CGO-free) works out of the box for `android/arm64` via `CGO_ENABLED=0 GOOS=android GOARCH=arm64 go build -tags mobile ./cmd/bot`. Output: a `18 MB` Go-runtime ELF aarch64 PIE executable that `file` identifies as "ELF 64-bit LSB pie executable, ARM aarch64 … dynamically linked, interpreter /system/bin/linker64". No NDK install was required.
- APK size delta vs the `gomobile bind` path: not measured against a real `gomobile bind` build (the POC was deliberately not built — see "Surprises" below). The 18 MB Go binary is the dominant cost regardless of approach; a `gomobile bind` `.aar` is in the same ballpark, plus the per-arch duplication.
- Cold start, foreground/background battery drain: deferred to Task 6 / Post-Completion real-device pass — only the embedded-HTTP build will be measured because `gomobile bind` is ruled out on the structural grounds below.
- Surprises:
  - **`android/arm` and `android/amd64` require CGO + NDK linking.** Only `android/arm64` builds cleanly without CGO; `GOARCH=arm` and `GOARCH=amd64` produce `android/<arch> requires external (cgo) linking, but cgo is not enabled`. Task 2's "verify all three target ABIs build clean" item must either drop `arm` and `amd64` (modern Android devices are arm64; armv7 is legacy; amd64 is emulator-only) or install the Android NDK and switch to `CGO_ENABLED=1` with an NDK-aware CC. Recommended: ship arm64 only for v1, document the constraint in `docs/local-mode.md`, and add the other ABIs in a follow-up once NDK is wired into the build pipeline.
  - **`gomobile bind` was not built.** The plan template asked for a POC, but the structural case for embedded HTTP is decisive enough that running a POC would not change the decision: `gomobile bind` exposes Go functions as JNI calls, which would require duplicating the entire HTTP/JSON transport surface as Kotlin/Java glue, breaking the "transports share domain services" invariant in CLAUDE.md and forcing every new endpoint to be added in two places. Time spent on a `gomobile bind` POC is better invested in finishing the embedded-HTTP path. This surprise (skipping the POC) is documented here so a future reviewer can revisit if circumstances change.

Rationale: embedded HTTP keeps `internal/server` as the single source of truth for the HTTP/JSON contract that both the browser PWA and the Capacitor WebView consume. The frontend bootstrap change ("read `apiBase` from injected global") is a one-line tweak. The Android shell only needs to spawn a process, parse one stdout line, and `loadUrl` — no JNI surface, no per-endpoint Kotlin wrappers, no `aar` build step. The compile pipeline is `go build` with extra env vars: same toolchain the server build already uses. Cross-compile size and the foreground-service lifetime are the only real Android-specific concerns; both are well-trodden ground (VPN clients, music players, fitness trackers all use the same pattern).

If `gomobile bind` ever becomes preferable (e.g. Google Play disallows process-spawn from APKs — currently allowed, but Play policy shifts every few years; or Android changes `nativeLibraryDir` exec semantics), revisit by re-reading this section first. The transport invariant is the load-bearing constraint, not the runtime mechanism — if a switch is forced, plan to extract the HTTP contract into a generated client (OpenAPI or similar) before swapping in `gomobile bind`, so the two-places-to-update problem is solved at the contract layer rather than at the call sites.

## Post-Completion

*Items requiring manual intervention or external systems — no checkboxes, informational only.*

**Manual verification on real Android device:**
- Install the debug APK via `adb install` and confirm:
  - app launches, WebView loads PWA from embedded Go within ~2s
  - Today view shows data, BP record persists, food log persists, all offline
  - swipe app to recents → return: same process reused, no reload
  - swipe app away: process stops within ~60s (confirm via `adb shell ps`)
  - airplane mode → app still works for local reads/writes
  - kill the Go process externally via `adb shell kill -9 <pid>` → next app foreground respawns it transparently
- Run for ~1 hour foregrounded and ~1 hour backgrounded; capture `dumpsys batterystats` and check the medtracker process is not in the top consumers (compare to expectations from Task 1's spike).

**Play Console / store-prep tasks (not required for an internal preview):**
- Eventually need a signed release APK + Play Console listing. Out of scope for this plan; capture under a future "store distribution" plan.
- App permissions audit — embedded localhost server does not need INTERNET permission for the server itself (loopback works without it) but the Capacitor WebView does for any external resources. Confirm permission list is minimal.

**Operator-side runbook updates:**
- None — this is an end-user mobile app, no operator role.

**Dependencies on follow-up plans:**
- Phase 2b (native plugins) consumes the working embedded shell — without 2a, plugins have nowhere to ride.
- Phase 2c (first-run setup + secrets) consumes the `EncryptedSharedPreferences` bootstrapping from Task 3.
