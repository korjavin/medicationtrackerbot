/bot
/mcptool
/seeddemo
/feedbackpull
/vendor/
meds.db
*.db
*.log
.DS_Store
*.db-wal
*.db-shm
docs/screenshots
.agent/rules/
.agent/workflows/
import.sql
frontendplan.markdown
node_modules/
coverage/

# ralphex progress logs
.ralphex/progress/

# ralphex progress logs
.ralphex/worktrees/
.claude/

# Local-only design reference (mockups, chat transcripts, screenshots with personal data)
# Referenced read-only by docs/plans/ — never commit.
.local/

# Python bytecode and coverage artifacts
__pycache__/
*.pyc
.coverage

# mcpeval scorecards (per-run output of cmd/mcpeval)
mcpeval-report.json
mcpeval-report.md

# Local secrets (API keys / prod endpoints) — e.g. .env.mcpeval for the eval loop
.env
.env.*

# Beads / Dolt files (added by bd init)
.dolt/
.beads-credential-key
.beads/proxieddb/
.beads/issues.jsonl
.beads/interactions.jsonl
/cloud
